{
  "eslint": {
    "commands": {
      "lint": "eslint .",
      "lint:fix": "eslint . --fix"
    }
  },
  "prettier": {
    "commands": {
      "fmt": "prettier --write .",
      "fmt:check": "prettier --check ."
    }
  }
}
